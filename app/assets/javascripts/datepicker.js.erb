<%
  def datepicker_locale
    I18n.locale == :'sr-Latn' ? 'sr-latin' : I18n.locale.to_s
  end
%>

$(document).on('page:change', function () {

  $('.datepicker').datepicker({
      format: 'yyyy-mm-dd',
      language: '<%= datepicker_locale %>'
    }
  );

});