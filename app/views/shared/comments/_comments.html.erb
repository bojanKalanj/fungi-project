<h1>Komentari (<%= commentable.comments.count %>)</h1>
<div class="well">
  <% if commentable.comments.any? %>
    <% commentable.comments.each do |comment| %>
      <div class="row">
        <div class="col-sm-1">
          <a href="#"><img class="img-round comment-img" src="<%= asset_path( 'index/vucic.jpg' ) %>" alt="Card image cap"></a>
        </div>
        <div class="col-sm-11">
          <%= link_to comment.user.full_name, user_path(comment.user), class: 'comment-link-user' %>
          <p class="text-muted"><%= comment.created_at.to_date %></p>
          <p class="comment-body"><%= comment.body %></p>
        </div>
      </div>
      <hr>
    <% end %>
  <% end %>
</div>
